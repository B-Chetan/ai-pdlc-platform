version: '3.8'

services:
  ai-pdlc-platform:
    build: .
    container_name: ai-pdlc-platform
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - ai-pdlc-network

networks:
  ai-pdlc-network:
    driver: bridge
